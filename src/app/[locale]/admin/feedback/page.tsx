import AdminFeedback from "@/app/admin/feedback/page";

export const dynamic = "force-dynamic";
export const metadata = {
  title: "Feedback — Embir Admin",
  robots: { index: false, follow: false },
};

export default AdminFeedback;
