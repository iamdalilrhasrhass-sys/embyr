import AdminAnalytics from "@/app/admin/analytics/page";

export const dynamic = "force-dynamic";
export const metadata = {
  title: "Cockpit — Embir Admin",
  robots: { index: false, follow: false },
};

export default AdminAnalytics;
